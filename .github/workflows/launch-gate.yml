name: Launch gate
on:
  pull_request:
  push:
    branches: [main, master]
jobs:
  placeholders:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No placeholder text may ship
        run: bash scripts/launch-gate.sh
